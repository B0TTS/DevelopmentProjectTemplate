# S2 fixture results — daily-trends-report skill

**Purpose:** S2 exit-gate log — the 7 success criteria 1–7 scenarios + the same-day rerun (criterion 10), built and run against the 3 audited scripts (S1 baseline: `b0ttsagent/temp/daily-trends-report-skill/script-review.md`). Verdict per scenario: PASS / FAIL / FIXED.
**Environment:** Python 3.14.6 on PATH (stdlib-only scripts). Deterministic fixtures with explicit `--today 2026-08-18` everywhere; 7-day window = 2026-08-11..2026-08-18. `generated_at` in inventory uses the canonical UTC−10 clock (A2).
**Scripts under test:** `.agents/skills/daily-trends-report/scripts/build-inventory.py`, `route-and-verify.py` (subcommands `route` / `verify`), `_lib.py`.

## Fixture layout

`b0ttsagent/temp/daily-trends-report-fixtures/` holds one base per scenario (`s<N>/AI-Development Trends/<YYYY-MM>/<YYYY-MM-DD>/`), generated by `make-fixtures.py` (idempotent; imports `_lib` so identity keys match the scripts exactly). Helper scripts beside it: `summarize.py` (routed/inventory summary), `inject-s7.py` (s7 failure-mode injection), `check-s8.py` (rerun determinism). Full command output per run: `logs/s<N>-*.txt`. Leaf JSONs use the wave-spec schema (`headline, why, url, published_date, domain, source_type, delta_or_null`); `swe.json` / `productivity.json` are empty-item files except s1.

Shared streak identity **X** = headline "Model X hits milestone", URL `https://example.com/model-x` (in s2's 08-17 prior written with `?utm_source=hn` to exercise URL normalization end-to-end).

Notes common to every verify run:
- The glance label `*Today at a glance*` + exactly 3 bullets is satisfied in every fixture → no glance warning.
- The word budget (~400–700) fires as a **soft warning** in every run because fixtures are intentionally compact. Warnings never affect the exit code (contract: nudges, not gates). Recorded, not failures.

---

## Scenario 1 — criterion 1: first-ever run (empty base)

Command (run in repo root; `<fx>` = the fixtures dir):
```
python .agents/skills/daily-trends-report/scripts/build-inventory.py --base "<fx>/s1/AI-Development Trends" --today 2026-08-18
python .agents/skills/daily-trends-report/scripts/route-and-verify.py route --folder "<fx>/s1/AI-Development Trends/2026-08/2026-08-18" --today 2026-08-18
python .agents/skills/daily-trends-report/scripts/route-and-verify.py verify --folder "<fx>/s1/AI-Development Trends/2026-08/2026-08-18" --today 2026-08-18
```
(build-inventory default `--out` used → writes into today's day folder.)

**Expected:** inventory scans 0 priors, no streak hits; all 3 candidates (one per leaf) route to main as fresh (`update: false`); a valid report.md passes verify (exit 0).
**Actual:** `scanned 0 prior report(s), 0 url-key streak hits, 0 headline-key hits, 0 warning(s)`; `main: AI trends 1, SWE trends 1, Productivity 1 | still_circulating: 0 | excluded: 0 | dropped: 0`; all three items `update=False`; verify `PASS: gates clean, 1 warning(s)` (word-budget nudge only), exit 0. Logs: `logs/s1-*.txt`.
**Verdict: PASS**

## Scenario 2 — criterion 2: identity in BOTH last 2 reports, re-proposed, no delta → Still circulating

Same 3 commands against `<fx>/s2/...`. Priors: `2026-08-16` and `2026-08-17` both carry X in `## AI trends`; today's `ai.json` re-proposes X (published 2026-08-14, `delta_or_null: null`) plus one fresh item.

**Expected:** X appears in **no main section**; it lands in Still circulating (≤7 days); the fresh item is main; verify passes.
**Actual:** `scanned 2 prior report(s), 3 url-key streak hits`; route: `main: AI trends 1 | still_circulating: 1` with X in SC and the fresh item in main; verify exit 0 (`PASS: gates clean, 1 warning(s)`). Inventory key check confirmed URL normalization: the 08-17 prior's `https://example.com/model-x?utm_source=hn` collapsed to streak key `https://example.com/model-x` (headline fallback also present). SC row date 2026-08-14 passed verify's window check.
**Verdict: PASS**

## Scenario 3 — criterion 3: streak-hit + published >7 days → excluded

Same 3 commands against `<fx>/s3/...`. Priors 08-16/08-17 carry X dated 2026-08-06; today re-proposes X (same date, no delta).

**Expected:** X appears nowhere (excluded, absent from main, absent from Still circulating); verify passes on a report without X.
**Actual:** route: `excluded: 1` with reason `older than 7 days and streak-hit`; `still_circulating: 0`; main holds only the fresh item; today's report.md (no X anywhere) → verify exit 0.
**Verdict: PASS**

## Scenario 4 — criterion 4: streak-hit with a dated, concrete delta → main, flagged update

Same 3 commands against `<fx>/s4/...`. Priors 08-16/08-17 carry X dated 2026-08-13. Today's `ai.json` proposes two updates:
- X, same URL, `published_date 2026-08-17`, `delta_or_null "v2.1 ships 2026-08-17 with 3x benchmark and new API"` (newer-date branch);
- X's headline on a **new URL** `https://example.com/model-x-v2-details`, same newer date + delta (headline-identity fallback → new-URL branch).

**Expected:** both route to main with `update: true`; verify passes via the update exemption (no collision error despite the streak).
**Actual:** route: `main: AI trends 2`, both items `update=True`; verify exit 0 — the collision check was skipped for both identities via `routed_update_ids`. Both update-test branches ((a) newer date and (a) new URL) exercised end-to-end.
**Verdict: PASS**

## Scenario 5 — criterion 5: published >7 days, no delta → absent everywhere

Same 3 commands against `<fx>/s5/...` (empty base — fresh, non-streak item). Today's `ai.json`: "Legacy feature announcement" published 2026-08-05 (no delta) + one fresh item.

**Expected:** the old item appears nowhere; fresh item is main; verify passes.
**Actual:** route: `excluded: 1` with reason `older than 7 days`; main holds only the fresh item; today's report.md contains no old item → verify exit 0.
**Verdict: PASS**

## Scenario 6 — criterion 6: undated finding → dropped/absent

Same 3 commands against `<fx>/s6/...` (empty base). Today's `ai.json`: "Mystery finding with no date" with `published_date: ""` + one fresh item.

**Expected:** the undated item is dropped and absent from the report; verify passes.
**Actual:** route: `dropped: 1` with reason `no verifiable YYYY-MM-DD published date`; main holds only the fresh item; verify exit 0.
**Verdict: PASS**

## Scenario 7 — criterion 7: main-section identity collision, update test NOT passed → verify exits 1

Commands against `<fx>/s7/...`:
1. `build-inventory` (prior 08-17 carries X dated 2026-08-13; exit 0).
2. `route` → honest routing puts X in still_circulating (re-proposed, no delta).
3. **Failure-mode injection:** `python inject-s7.py <day>/routed.json` moves X from SC into `main["AI trends"]` with `update: false` — simulating a routed.json that claims a streak-hit for main without a passing update test (stale or hand-edited routed file). This is the state verify's collision gate must catch.
4. `verify` against the writer-error report.md (X present in `## AI trends`, dated 2026-08-13 — in window, so collision is the *only* gate violation).

**Expected:** verify exits 1 with a streak-collision error naming the prior file.
**Actual:** `ERROR: AI trends: streak collision — 'Model X hits milestone' appeared in 2026-08/2026-08-17/report.md and the update test did not pass`; `FAIL: 1 gate violation(s), 1 warning(s)`; exit 1. The positive control (update-flagged item in main → exempt) is scenario 4's verify.
**Verdict: PASS**

## Scenario 8 — criterion 10: same-day rerun

Commands against `<fx>/s8/...` (prior 08-17 carries "Recurring story from yesterday"; today's folder already holds a first-run `report.md` with today's fresh item T, `ai.json` = [T]):
Run 1: build-inventory → route → verify (all exit 0). Snapshots saved.
Run 2: identical three commands (all exit 0). Day-folder outputs compared.

**Expected:** inventory ignores today's folder (even though today's `report.md` exists with its own identity); outputs overwrite in place; the second run produces identical routing.
**Actual:**
- `routed.json` **byte-identical** across runs (no timestamps inside).
- `inventory.json` identical modulo `generated_at` (`2026-08-18T18:00:09…-10:00` vs `…18:00:21…-10:00`).
- `reports_scanned: ['2026-08/2026-08-17/report.md']` — today's folder ignored.
- `streak_hits` contains `https://example.com/s8-prior` and **not** `https://example.com/s8-today` — today's report identity never entered history.
- Both verifies: `PASS: gates clean, 1 warning(s)`, exit 0. (Overwrite semantics: `write_text` replaces in place; second-run outputs are the live files, verified identical to run-1 snapshots.)
**Verdict: PASS**

---

## Script fixes

**None.** All 8 scenarios produced the contract-expected behavior on the S1-fixed baseline; no script was modified in S2. `python -m py_compile` re-run clean on all 3 scripts (exit 0) per the exit gate.

Fixture-side fixes during setup (fixture bug, not script bug): `make-fixtures.py` `write_leaf` initially treated its first arg inconsistently (Path vs int) → normalized inside the helper; no script touched.

## Criteria 8–10: script-checkable vs SKILL.md instruction checks

| Criterion | Script-checkable (fixture evidence) | SKILL.md instruction check (manual) |
|---|---|---|
| 8 — index.md row + chat pointer | none scripted (BY-DESIGN, RESEARCH §2: index upsert and chat pointer are orchestrator steps; deterministic layer = the 2 scripts) | index.md created-if-missing with newest-row-on-top, upsert-not-append on rerun; chat = path + 3 glance bullets + "N items demoted" when SC non-empty |
| 9 — glance readable in <1 min, file in 15 min | partial: glance label + exactly-3-bullets and word budget are verify **warnings** (exit 0) — satisfied in every fixture | reading-time checks are human; markdown-doc-designs quality bar applies in S3/S5 |
| 10 — same-day rerun | fully script-checkable: **s8 PASS** (overwrite + today's-folder exclusion + identical routing) | the "index.md still has one row for the day" half rides on the criterion-8 instruction |

## Exit-gate checklist

1. ✅ All 7 mapped scenarios + same-day rerun produced expected outcomes (criteria 8–9 split recorded above).
2. ✅ `results.md` exists (this file) with per-scenario command, expected, actual, PASS/FAIL/FIXED; no script fixes to log.
3. ✅ No scripts fixed in S2; `py_compile` re-run clean anyway (exit 0).

## Notes for S4 (eval-fixtures.md)

- Every scenario is reproducible from `make-fixtures.py` + the command lines quoted above (all outputs re-created in `logs/`).
- s7's injected failure mode is deliberate fixture engineering, documented in `inject-s7.py`; the honest pre-injection route output is in `logs/s7-route.txt`.
- The 5 mandated minimum fixtures (CONTEXT-v2 §Skill Shape authoring order) are covered by s1, s2, s3/s5, s6, s7.
- Still-circulating cap/sort (14→10, newest first) and domain-preference notes were proven in S1 probe2 and are not re-duplicated here; s2 covers the SC happy path end-to-end.
